You can now choose which model analyses your food. Settings offers four tested options — fastest, balanced, open-weight and most accurate — each labelled with figures from the app's own test set, plus an advanced field for any OpenRouter model id, marked untested. The default is unchanged. Accuracy improved for everyone too: a bug meant the app never sent its temperature setting, and fixing it cut average calorie error by six points. Retry with a stronger model shows only where one exists.
